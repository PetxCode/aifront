services:
  ai-event-fe:
    build: .
    container_name: ai-event-fe
    ports:
      - "3000:80"
    restart: unless-stopped
    networks:
      - ai-event-network

networks:
  ai-event-network:
    external: true
